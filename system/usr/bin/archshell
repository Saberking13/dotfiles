#!/bin/bash

CHROOT="/var/chroot/archlinux"

archcommand=$(echo $@ | tr ' ' '\ ')

[[ -z $@ ]] && doas chroot $CHROOT /bin/su lemniskett \
  && neofetch \
  || doas chroot $CHROOT su -c "$archcommand" lemniskett
